node_modules/
dist/
dev-dist/
coverage/
playwright-report/
test-results/
.shots/
.vite/
*.local
.DS_Store

# Throwaway probes run from the repo root, because `tsx` needs them inside the
# project to resolve `./src/...`. They are not part of the game.
*.tmp.ts
*.tmp.mjs
*.tmp.js
.env
.env.*
!.env.example

# Screenshot gallery output (`npm run gallery`); published as a CI artefact, never committed.
/gallery/
