# Riverside activity dock

- **Updated:** 2026-09-19T11:58:37Z; outgoing owner `/root/world_conversations`; incoming integration owner `/root`.
- **Outcome:** The Riverside bottom dock now shows a concise status, the three primary forms, an explicit `Activities` disclosure, and `Follow party` plus preview exit. Secondary visits, the Ba Dan return, Dorin's drill, and preview battle remain available inside the opened activity group. Header journal and pause remain the campaign utilities; Settings remains reachable through Pause.
- **Acceptance:** At 1280×720 the closed dock is under 180px, the live map is over 480px tall, and the Riverside panel has no horizontal action scroller. Opening `Activities` exposes every secondary action with `aria-expanded`/`aria-controls`; the dock itself remains the scroll surface for narrow and Largest-text layouts. Existing form, visit, discovery, drill, preview exit, camera-follow, campaign-return, and pointer-world interactions remain functional.
- **Location:** `C:\Users\Jared\.codex\worktrees\riverside-dock`, branch `codex/riverside-dock`, source commit `a5b6681fc30a85e9156a00e166a3ceef7a82d666`, based on `9a54d23addf96ebd877ca6ed718c1784e3714fd8`. No PR, push, version bump, or CI run was made.
- **Worktree state:** Source and test changes are committed; the worktree is clean. Ignored local configs are `.shots/riverside-dock-local.config.ts`, `.shots/riverside-world-local.config.ts`, and `.shots/riverside-webkit-local.config.ts`, all using strict port `4295`. The installed Chrome and WebKit runs exited; no listener remains. The local `node_modules` junction points to the existing route-art-followup dependency tree and is not tracked.
- **Completed:** `src/app/village/VillageLife.ts` removes duplicated Riverside journal/settings/pause controls, keeps campaign header/Pause ownership, adds the accessible Activities disclosure, and keeps Follow party/Leave preview reachable. Riverside-specific selectors in `src/styles/hud.css` wrap actions, remove horizontal overflow, and let the dock scroll when the open menu exceeds the viewport. `e2e/00-riverside.spec.ts`, `e2e/world.spec.ts`, and `e2e/gallery/beats.ts` cover the new control entry point.
- **Decisions:** The implementation reuses the existing DOM/button, HUD, and dock scroll patterns. No modal stack or new interaction framework was introduced. Menu state is retained by the existing `VillageLife` instance across flag refreshes, and test helpers open it only when `aria-expanded` is false. World click picking and the VillageLife reducer paths are unchanged.
- **Verification:** `npm run verify` passed at `a5b6681fc30a85e9156a00e166a3ceef7a82d666` (typecheck, lint, format check, 102 unit files / 844 tests). Installed Chrome on port 4295 passed the compact 1280×720 Canvas case, Largest-text 390×844 portrait case, preview-close case, rescued Riverside return, and WebGL form/visit/drill campaign-preservation case. Installed WebKit iPad passed the Largest-text portrait case. The open-menu test confirms the secondary visits, shrine, and Dorin controls are reachable and each remains at least 48px tall. Physical-device review is not covered.
- **Coordination:** Scope is limited to `VillageLife`, Riverside-specific HUD CSS, and Riverside/gallery tests. It does not touch `ExploreScene`, CombatScene, maps, choreography, story, or root's separate conversation camera fix `1f82c1c`. The existing Tea break copy/pose mismatch (the message says the party sits while the Sura figure remains standing by the porch) was observed and left for a separate task.
- **Next actions:** Root should cherry-pick `a5b6681fc30a85e9156a00e166a3ceef7a82d666`, review the closed/open dock on the combined release, and run the integrated required checks. Keep port 4295 available for another owner after confirming no listener.
- **Completion/transfer:** Pending integration by `/root`; outgoing owner has relinquished editing ownership after this handoff.
