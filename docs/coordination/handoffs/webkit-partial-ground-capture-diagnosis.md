# WebKit partial-ground capture diagnosis handoff

- **Updated:** 2026-09-19T23:36:00Z; outgoing owner `/root/partial_ground_validation`; integration owner `/root`.
- **Outcome:** Diagnosed the local Windows Playwright WebKit failure as a browser presentation/capture boundary. No product, renderer, or assertion change is included.
- **Acceptance:** Separate browser screenshot evidence from framebuffer/state evidence, and do not treat the local failure as proof that partial-ground restoration is incorrect.
- **Location:** `C:\\Users\\Jared\\.codex\\worktrees\\webkit-reentry-diagnosis`, branch `codex/webkit-reentry-diagnosis`, clean head `d0bf98c12ca5e98fd2108a6e6663da2c3e709b2a`; no PR or push.
- **Worktree state:** clean; generated Playwright failure output remains ignored under `test-results/`; no preview server is running.
- **Completed:** Fresh installed WebKit (`FNT_E2E_WEBKIT=1`, `ipad-landscape`) ran `partial ground keeps an opaque patch under permanent water on webgl`. It failed at the initial authored-colour readiness poll after 60 seconds, before water removal or scene re-entry. Local evidence is `test-results/partial-ground-partial-gro-69fb5-er-permanent-water-on-webgl-ipad-landscape/test-failed-1.png`: the gameplay canvas is blank while the HUD and minimap are present.
- **Decisions:** Earlier durable diagnosis in [modular-route-followup](modular-route-followup.md) measured a populated 2388×1274 WebGL framebuffer (`gl.readPixels`: 2,726,468 covered pixels and about 129,600 blue pixels; GL error zero) while the immediately following browser screenshot was blank. The standalone bare-WebGL resize control recorded in [riverside-webgl-resize](riverside-webgl-resize.md) also loses screenshots without Pixi. That separation makes a renderer/state repair speculative and likely to hide the browser boundary rather than fix the observed capture failure.
- **Verification:** Fresh local failure at `d0bf98c`: `FNT_E2E_WEBKIT=1 npx playwright test e2e/partial-ground.spec.ts -g "partial ground keeps an opaque patch under permanent water on webgl" --project=ipad-landscape`; Playwright reports `waitForSamples` timeout at `e2e/partial-ground.spec.ts:222`. No source check was needed because source is unchanged.
- **Coordination:** `/root` owns the separate `release_local_regression` restoration-poll repair and integration. This diagnosis did not edit that poll. CI `35474090671` is Linux Chromium Surface WebGL; it timed out in the elevated-surface restoration poll, so it is not evidence of this Windows WebKit screenshot boundary.
- **Next actions:** Run the unchanged pixel assertions in the required Linux CI after root's test-only restoration-poll repair. Treat a Linux pass/failure as the discriminator for release behavior; do not modify product rendering or weaken screenshot assertions based on this Windows WebKit artifact.
- **Completion/transfer:** Diagnosis complete; outgoing owner relinquishes editing ownership.
