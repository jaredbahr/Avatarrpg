# Exploration guidance and solo dock handoff

- **Updated:** 2026-09-18T22:31:30-05:00; gameplay integration owner; incoming owner not assigned.
- **Outcome:** The Local Map derives its optional NPC guidance from the current objective and exposes a real `previewWalk`-validated `Walk to <NPC>` action. A one-member exploration dock hides unnecessary vertical overflow at the normal 1280x720 landscape size while Large/Huge text retains scrolling. The accepted factual elemental setup-copy source is included. This checkpoint does not claim visual acceptance or physical-device validation.
- **Acceptance:** Opening the village Local Map labels Elder Mira and enables `Walk to Elder Mira`; the action closes the dialog and routes through the existing walk reducer. A normal solo dock reports `overflow-y: hidden` without scroll range; Huge text reports `overflow-y: auto`.
- **Location:** Avatar RPG, worktree `C:/Users/Jared/Documents/ChatGPT/Avatar RPG-exploration-guidance`, branch `codex/exploration-guidance`, base `origin/main` at `44ed3f6`; no PR opened. Accepted source `efea355711180b9fa19d008a8668b9558f8a0e21` was cherry-picked as `15fe278`.
- **Worktree state:** Source and test edits are uncommitted on top of `15fe278`; the temporary ignored `chrome-local.tmp.ts` Playwright config is local-only and must not be committed. No source changes were made to the frozen PR #64 candidate.
- **Completed:** `src/app/ui/LocalMap.ts` renders dialog labels and objective-aware pathfinder guidance; `src/app/scenes/ExploreScene.ts` passes the real content/objective and marks solo docks; `src/styles/hud.css` styles labels and overflow behavior; `e2e/local-map.spec.ts` covers both focused behaviors; setup copy and its source handoff came from `15fe278`.
- **Decisions:** Target selection uses the existing `worldObjective` string and NPC data, and route validity uses `previewWalk`; no duplicate objective or movement rules were added. Large-text overflow remains available for accessibility.
- **Verification:** On the current uncommitted source checkpoint, `npm run verify` passed: typecheck, lint, format check, 72 test files / 689 tests. Chrome-channel focused Playwright checks passed on 1280x720: `local map tracks a real walk and preserves the campaign when opened` (1 passed) and `a normal solo exploration dock has no phantom vertical scroll while large text keeps it available` (1 passed). The focused run used `chrome-local.tmp.ts`; CI Chromium/WebKit and physical-device checks remain outstanding. A new commit will require fresh CI.
- **Coordination:** Keep PR #64 (`7b052f1` release candidate) frozen. This branch owns only the local-map guidance, solo dock overflow, and accepted setup copy. Do not add art/map-scene registrations or unrelated presentation work. Coordinate the checkpoint with orchestrator `01a0b79f-a1e6-7fd0-a4d5-76e7944406f6`.
- **Next actions:** Review the diff, commit the source checkpoint, and decide whether to open a separate PR from current `main`. If published, remove the temporary config and run CI on the new exact head; repeat the focused map and dock checks in the repository's normal browser configuration.
- **Completion/transfer:** Pending local checkpoint commit and orchestrator handoff; editing ownership remains with the current gameplay integration owner until transferred.
