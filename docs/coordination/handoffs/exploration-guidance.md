# Exploration guidance and solo dock handoff

- **Updated:** 2026-09-18T22:46:30-05:00; gameplay integration owner; incoming owner not assigned.
- **Outcome:** The Local Map derives its optional NPC guidance from structured objective metadata and exposes a real `previewWalk`-validated `Walk to <NPC>` action. After Mira's conversation, the same objective source switches to the east-road guidance using the existing visited condition. A one-member exploration dock uses intrinsic landscape sizing at normal 1280x720 while Large/Huge text retains its auto overflow behavior; six-party rosters retain horizontal scrolling. The accepted factual elemental setup-copy source is included. This checkpoint does not claim visual acceptance or physical-device validation.
- **Acceptance:** Opening the village Local Map labels Elder Mira and enables `Walk to Elder Mira`; clicking it reaches the real `mira_intro` conversation through the existing reducer/pathfinder. Reading the conversation returns to the village with the objective changed to taking the east road and no Mira walk target. A normal solo dock's roster and action bounds fit the 1280x720 viewport without a vertical scroll range; narrow portrait Huge keeps auto overflow available; six-party roster remains horizontally scrollable.
- **Location:** Avatar RPG, worktree `C:/Users/Jared/Documents/ChatGPT/Avatar RPG-exploration-guidance`, branch `codex/exploration-guidance`, local HEAD reported with `git rev-parse HEAD` at transfer, base `origin/main` at `44ed3f6`; no PR opened. Accepted source `efea355711180b9fa19d008a8668b9558f8a0e21` was cherry-picked as `15fe278`.
- **Worktree state:** Source checkpoint is committed locally and the worktree is clean; the temporary ignored `chrome-local.tmp.ts` Playwright config is local-only and must not be committed. No source changes were made to the frozen PR #64 candidate.
- **Completed:** `src/app/ui/LocalMap.ts` renders visible-NPC labels and structured objective pathfinder guidance; `src/app/scenes/ExploreScene.ts` passes the objective target and marks solo docks; `src/core/story/world.ts` and `src/core/types.ts` provide structured objective target metadata, visited objective variants, and a shared NPC visibility predicate used by UI, walking preview, and reducer; `src/styles/base.css` and `src/styles/hud.css` use SVG user-unit tokens and intrinsic dock sizing; `e2e/local-map.spec.ts` covers the click-through, post-Mira target transition, and normal/Huge/six-party bounds; setup copy and its source handoff came from `15fe278`.
- **Decisions:** Target selection uses `objectiveNpcId` on the active explore node rather than parsing display prose. Objective text and target metadata share the same conditional variant source, using `visited('mira_intro')` without a new save flag. Visibility is evaluated from the same optional NPC condition in the Local Map, movement preview, scene markers, and reducer. Route validity uses `previewWalk`; no duplicate objective or movement rules were added. Large-text overflow remains available for accessibility.
- **Verification:** `npm run verify` passed before the final checkpoint commit: typecheck, lint, format check, and 72 test files / 689 tests. Chrome-channel focused Playwright checks passed on 1280x720 plus narrow portrait and six-party cases (2 passed), including a click-through to `mira_intro`. The focused run used `chrome-local.tmp.ts`; CI Chromium/WebKit and physical-device checks remain outstanding. This exact head has no pushed CI.
- **Coordination:** Keep PR #64 (`7b052f1` release candidate) frozen; its separate E2E job currently has a screenshot timeout and is being repaired by its assigned specialist. This branch owns only the local-map guidance, solo dock overflow, and accepted setup copy. Do not add art/map-scene registrations or unrelated presentation work. Coordinate the checkpoint with orchestrator `01a0b79f-a1e6-7fd0-a4d5-76e7944406f6`.
- **Next actions:** Send the exact SHA to the orchestrator. If published later, run fresh CI on that head; do not reuse earlier checks. Remove the temporary config before any publication.
- **Completion/transfer:** Local checkpoint complete and handed off; editing ownership remains with the current gameplay integration owner until transferred.
