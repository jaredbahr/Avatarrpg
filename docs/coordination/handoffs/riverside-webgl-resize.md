# Riverside WebGL resize presentation diagnosis

- **Updated:** 2026-09-19; integrated by root at `6e72cfd` from source `c4229e1`; outgoing owner `/root/combat_preview`; incoming owner `/root`.
- **Outcome:** Isolated the Riverside Activities resize symptom to the Windows Playwright WebKit canvas presentation surface. The Pixi framebuffer and `toDataURL()` remain populated immediately after every resize render; a bare WebGL canvas reproduces the same screenshot loss. No ineffective renderer reset or product workaround is included.
- **Acceptance:** The diagnostic reproduces the failure with a standalone WebGL control and, when pointed at a running preview, the Riverside route. It reports synchronous framebuffer pixels, synchronous data URL metrics, and the later element screenshot separately. The result is explicitly limited to the Windows WebKit 2359 path.
- **Location:** `C:\Users\Jared\.codex\worktrees\riverside-webgl-resize`, branch `codex/riverside-webgl-resize`, based on `b69fa4e`; no PR, push, version bump, or CI run.
- **Worktree state:** The diagnostic and this handoff are the only tracked changes. The local preview was served on port `4276`; ports `4276` and `4268` are confirmed stopped. Durable captures are under `C:\Users\Jared\.codex\visualizations\2026\09\19\01a0b79f-a1e6-7fd0-a4d5-76e7944406f6\riverside-resize\final-diagnostic`.
- **Completed:** Added [`scripts/diagnostics/riverside-webgl-resize.mjs`](../../../scripts/diagnostics/riverside-webgl-resize.mjs). It runs a bare WebGL canvas resize control and optionally drives Riverside with `FNT_RESIZE_BASE_URL`, `FNT_WEBKIT_EXECUTABLE`, and `FNT_RESIZE_OUTPUT`. Riverside's wrapper calls `gl.readPixels()` and `canvas.toDataURL()` synchronously inside the same `renderer.render()` call after each new backing-store size. No `src/` file remains changed.
- **Decisions:** `preserveDrawingBuffer`, filter detachment, texture/source refresh, sprite replacement, extra renders, Pixi state resets, and CSS layer flush variants were tested locally and did not restore the WebKit screenshot. The standalone control fails without Pixi, so adding any of those to the renderer would obscure a browser boundary without fixing the observed path. This does not claim Safari hardware or every WebKit build behaves the same way.
- **Verification:** On Windows Playwright WebKit executable `C:\Users\Jared\AppData\Local\ms-playwright\webkit-2359\Playwright.exe` (reported engine version `26.6`), with `npm run preview -- --host 127.0.0.1 --port 4276`:
  - `FNT_WEBKIT_EXECUTABLE=... FNT_RESIZE_BASE_URL=http://127.0.0.1:4276 FNT_RESIZE_OUTPUT=... node scripts/diagnostics/riverside-webgl-resize.mjs` completed with no page or console errors.
  - Standalone initial and resized framebuffer center pixels were both `[46,158,209,255]`; data URLs both had `meanRgb: 137.67`. Screenshots were `meanRgb: 137.67` before and `215.67` after resize, with `nonBackground: 0` after resize.
  - Riverside initial/opened/closed framebuffer pixels were `[246,193,108,255]`, `[247,194,109,255]`, `[246,193,108,255]`, all with WebGL error `0`; synchronous data URLs had `meanRgb: 123.01`, `125.77`, and `123.01`. Element screenshots were `meanRgb: 129.69`, `200.27`, and `199.35`; the opened/closed screenshots are the pale surface seen in the report. Captures are in the durable directory above.
  - `npm run verify` passed with 103 test files / 852 tests, and `npm run build` passed; the final edit after those checks only corrected this handoff's relative link. The product source is unchanged from the verified base. Physical Safari/iPad hardware was not tested.
- **Coordination:** Renderer ownership remains with `/root`; the diagnostic does not overlap the partial-ground work in `world_conversations` or the Riverside tea work. Root should not cherry-pick a renderer workaround from the abandoned experiments. The diagnostic script is the durable reproduction; the captured JSON and PNGs are local review evidence.
- **Limitation:** Page/console errors and WebGL error values are recorded in the JSON result but are not a pass/fail gate; inspect the JSON explicitly. This remains limited to the observed Windows headless WebKit path and makes no hardware Safari claim.
- **Next actions:** Root can run the script against another Windows WebKit build or a visible browser to establish whether the boundary is specific to this headless build. If the real device also loses presentation, investigate a browser-supported canvas replacement or renderer fallback as a separate design decision; do not infer it from this headless result alone.
- **Completion/transfer:** Root integration is recorded at `6e72cfd`; outgoing owner relinquishes the resize implementation scope, with no server remaining on ports `4276` or `4268`.
