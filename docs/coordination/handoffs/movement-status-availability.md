# Movement status availability handoff

- **Updated:** 2026-09-19 10:43 UTC; outgoing owner `world-conversations`; incoming integration owner `/root`.
- **Outcome:** Combat movement controls now use the existing `canMove` status rule. A party unit with an authored movement-preventing status such as Rooted has a disabled Move action with the status name and description, no reachable movement overlays, and no stale movement confirmation. The out-of-range ability recovery button, “Move instead,” follows the same guard and reason.
- **Acceptance:** Rooted Riko cannot enter move mode through Move or Move instead, cannot preview reachable tiles, and sees `Rooted: Stuck in place, but can still attack.` The reducer remains the final gameplay guard.
- **Location:** `Avatar RPG`, worktree `C:\Users\Jared\.codex\worktrees\move-status-availability`, branch `codex/move-status-availability`, base `dfadbd2370ec9167bebc7d4c1d89c63bb590ffda`, source commit `a563a8d1a93a9ab5846e99c1ceb23f73479fdc24`; no PR or push.
- **Worktree state:** Source commit is clean before this handoff document; the only ignored local artifacts are the Chrome config under `.shots/movement-status-local.config.ts` and the shared `node_modules` junction. No owned server or port 4294 listener remains.
- **Completed:** `src/app/scenes/CombatScene.ts` derives the UI reason from `canMove` and `statusDefs`, clears stale move mode during HUD sync, guards selection/confirmation/aim recovery, and suppresses blocked movement overlays. `e2e/combat-preview.spec.ts` adds a saved-state UI fixture for a Rooted Riko at `(1,3)`, covering Move and the disabled Move instead path. `directAttackThreats`, reducer rules, camera, renderer, and gameplay state transitions were not changed.
- **Decisions:** Keep the check inline in CombatScene to preserve the current shipped-JS headroom. Reuse `src/core/rules/stats.ts` and authored status descriptions instead of adding a new availability module or duplicating rule defaults.
- **Verification:** At source commit `a563a8d1a93a9ab5846e99c1ceb23f73479fdc24`, `npm run verify` passed: typecheck, lint, formatting, 97 Vitest files, 831 tests. Focused installed-Chrome production preview passed: `npx playwright test --config .shots/movement-status-local.config.ts --project surface-touch-local --grep "movement prevention"` — 1 passed in 19.5s, Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe`, SwiftShader flags, strict port 4294. `git diff --check` passed.
- **Coordination:** Parent owns integration and final build/budget checks. Cherry-pick source commit `a563a8d1a93a9ab5846e99c1ceb23f73479fdc24`, then this handoff commit. No CombatScene overlap remains with the independent `directAttackThreats` review.
- **Next actions:** Re-run the combined production batch and bundle check after integration. Keep the fixture as a UI contract test; it stages only the status/position needed to exercise the control path and does not claim a full campaign playthrough.
- **Completion/transfer:** Pending parent integration; outgoing owner has relinquished editing ownership after the handoff commit.
